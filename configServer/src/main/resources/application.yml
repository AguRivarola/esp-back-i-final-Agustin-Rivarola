server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AguRivarola/esp-back-i-final-Agustin-Rivarola
          default-label: main
          force-pull: true
          search-paths: config-data
